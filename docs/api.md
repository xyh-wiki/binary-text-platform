# API 说明（简要）

后端入口：`binary-text-backend` 模块，核心 Controller：

- `POST /api/extract/single`
- `POST /api/extract/batch`

具体请求 / 响应字段可直接参考 `backend/binary-text-backend/src/main/java/.../BinaryTextController.java` 中的注释。
