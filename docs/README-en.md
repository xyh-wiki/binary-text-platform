# Binary Text Platform · Backend & Frontend

This repo hosts the **binary file text extraction platform**, including:

- A multi-module Java backend (`backend/`)
- A React + Vite frontend (`frontend/`)
- Deployment resources for Dokploy / Docker / Traefik (`deploy/`)
- Documentation (`docs/`)
