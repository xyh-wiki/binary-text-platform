# Binary Text Platform

这是「二进制文件纯文本抽取平台」的统一仓库，包含：

- `backend/`：Java 后端（多模块：抽取核心库 + Spring Boot API）
- `frontend/`：前端站点（binaryextract.xyh.wiki）
- `deploy/`：Dokploy / Docker / Traefik 相关配置
- `docs/`：文档（架构、接口、SEO 等）

详见 `docs/README-zh.md` 与 `docs/README-en.md`。
